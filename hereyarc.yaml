name: hereya/aws-app-lambda
version: 0.2.1
description: API Gateway + Lambda + CloudFront-fronted Astro frontend, single distribution with /api/* APIGW behavior. Non-blocking ACM cert via custom resource; aliases attached on second deploy after cert validates. Optional deploy-time migration Lambda gated by a CloudFormation Custom Resource.
iac: cdk
infra: aws
deploy: true
visibility: public
